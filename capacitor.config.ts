import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'com.warhex.aistudio',
  appName: 'WarHex AI Studio',
  webDir: 'mobile',
  bundledWebRuntime: false,
  android: { backgroundColor: '#070a12' },
};
export default config;
